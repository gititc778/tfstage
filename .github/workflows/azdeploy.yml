name: "Deploy_Azure"
run-name: "${{ inputs.module }} Deploy"

on:
  workflow_dispatch:
    inputs:
      module:
        description: 'Module to deploy (All, Databricks, Data Factory, Synapse, Functions, CosmosDB, EventHub)'
        required: true
        default: 'All'
        type: choice
        options:
        - All
        - Databricks
        - Data Factory
        - Synapse
        - Functions
        - CosmosDB
        - EventHub

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform:
    name: "${{ inputs.module == 'All' && 'Terraform Deploy ALL' || format('Terraform Deploy {0}', inputs.module) }}"
    runs-on: ubuntu-latest
    env:
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: |
          # Map display names to internal module names
          declare -A MODULE_MAP=(
            ["All"]="all"
            ["Databricks"]="databricks"
            ["Data Factory"]="data_factory"
            ["Synapse"]="synapse"
            ["Functions"]="functions"
            ["CosmosDB"]="cosmosdb"
            ["EventHub"]="eventhub"
          )
          
          SELECTED_MODULE="${MODULE_MAP[${{ inputs.module }}]}"
          
          echo "Selected Input: ${{ inputs.module }}"
          echo "Internal Module: $SELECTED_MODULE"
          
          if [ "$SELECTED_MODULE" == "all" ]; then
            echo "Running full plan..."
            terraform plan -out=tfplan
          else
            echo "Running targeted plan for: module.$SELECTED_MODULE"
            terraform plan -target=module.$SELECTED_MODULE -out=tfplan
          fi

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
